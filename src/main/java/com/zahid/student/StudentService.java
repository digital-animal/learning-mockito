package com.zahid.student;

public interface StudentService {
    int getTotalMarks(); // let's assume fetches marks from database   
    int getTotalStudents(); // let's assume fetches student count from database   
}
